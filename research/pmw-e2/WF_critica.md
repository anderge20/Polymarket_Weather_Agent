CRÍTICA DE COMPLETITUD — Polymarket_Weather_Agent (lectura READ-ONLY, 2026-09-05 ~21:45)

Convención: OBSERVADO = leído en fichero/comando ahora; INFERIDO = deducción mía.

## A. El "estado_actual" del roadmap está desactualizado (verificado, 6 afirmaciones)

1. **El canónico ya no está en main@5287122 limpio.** OBSERVADO: `/Users/mariaaleu/workspace/Polymarket_Weather_Agent` está en la rama `codex/prices-ingestion-wip` @ `6ed85ad` (amend de `bcd9c1e`, "Rescue Codex WIP"), pusheada a `origin/codex/prices-ingestion-wip`; `main` sigue en 5287122. El commit contiene `src/weather_agent/polymarket/prices.py` (148 líneas), `tests/test_market_asof.py` (+82/−9), `PHASE_2E_SETTLEMENT_OPERATOR_DESIGN.md` y `polymarket/__init__.py`. `diff` contra el worktree: idénticos (SAME_prices, SAME_2E). Consecuencia: **R0 está hecho en 3 de 4 ficheros**; solo falta `PHASE_2E_LEAD_HOURS_ANCHOR.md` (`ls` en el canónico: "No such file"). El worktree `/private/tmp/pmw-publish/worktree` sigue sucio pero ya no es la única copia.
2. **Baseline pytest ya no es 127/4.** OBSERVADO: `PYTHONPATH=src python3 -m pytest tests/ -q -rs` → **131 passed, 2 skipped**; los únicos skips son `tests/test_weather_asof.py:26,33`. `tests/test_market_asof.py` tiene 4 tests reales (l.46,65,81,89), sin `skip`. **R9 lista "un-skip test_market_asof" como trabajo pendiente: ya está hecho.** Los criterios "pytest sigue en >=127 passed" (R0, R6) deberían decir 131.
3. **D3 no está "EN CURSO": está BLOQUEADO EXTERNAMENTE.** OBSERVADO `~/pmw-e2/DECISIONS.md:49-57`: Codex respondió `usage_limit_exceeded`, "try again at Oct 5th, 2026"; `CODEX_REPLY_01.err` = "sin respuesta tras 600s"; existe **D5** (l.59-69: Claude decide en solitario con refutadores adversariales) y **D6** (l.82-98: refutación adversarial de D1 ejecutada). **R1 tal como está redactado es inejecutable hasta 2026-10-05**, y los 8 ítems con `owner_sugerido: "ambos"` o "Codex" (R0, R5, R6-R14, R16-R17, R19-R24) no tienen ejecutor real. El roadmap debe reasignar owners bajo D5 o declarar la dependencia de Codex como bloqueo hasta octubre.
4. **R2 está esencialmente hecho.** OBSERVADO: `v5uni.py:7` carga `SNAP` desde `STATION_COORDS_SNAPSHOT_v1.json` y `:43-44` usa `SNAP[st]` con `src="STATION_COORDS_SNAPSHOT_v1"`; la línea 6 solo conserva `V3` para la `tz` (l.45). `PREREG_MODELSEL_V5.md:417-423` ya contiene la **ENMIENDA V5.3** (§14 alineado con D1) y `PREREG_MODELSEL_V5.sha256` = `e8302dc1…` coincide con disco. Lo que queda de R2: (a) `V5_STATUS.md` (mtime 15:47) sigue citando `v5comp.py`, "3 200 peticiones" y hashes v1/v1.1 (l.13-17, 31, 71); (b) `PREREG_MODELSEL_V5.md:218` conserva el texto original "Coordenadas: OurAirports" (la enmienda se apila, no lo reescribe — correcto por preregistro, pero hay que leer §14+V5.3 juntos); (c) el criterio "no hay referencia a req_lat de V3_SAMPLE" fallaría por la l.6 aunque sea inocua; (d) validación de 2-3 pares antes de las 4 000 sigue ausente en `v5run.sh` (3 líneas, sin smoke).
5. **D1 ya está "ADOPTADA y VERIFICADA" y el snapshot es v1.1, no v1.** OBSERVADO: `STATION_COORDS_SNAPSHOT_v1.json` tiene `schema: station_coords_snapshot/v1.1`, 54 CANONICA + 1 EXCEPCION_ABIERTA (OPKC); WSSS resuelta; sha en disco `071b142c…`. **Nueva discrepancia de custodia (no listada en R3):** `DECISIONS.md:30` y `PREREG_MODELSEL_V5.md:375` (V5.2) citan `ebe21014…`, hash que ya no corresponde a ningún fichero en disco (no existe `STATION_COORDS_SNAPSHOT_v1.0*`): el snapshot se sobrescribió in situ, contra D0 "nunca borrar" en espíritu. R3 debe incluirlo y R7 debe cargar 54/1, no 53/2. Además D6 (l.92-95) acota: solo 11/55 verificadas (10 ASOS EE.UU. + WSSS); 43 estaciones no-EE.UU. siguen INFERIDAS y KBKF no verificada. La decisión abierta "premisa NOAA = sensor no verificada" queda parcialmente cerrada y debe reescribirse.
6. **Vigilante:** OBSERVADO `ps`: PID 8074 `/bin/sh ./quota_watch.sh` activo; su línea de comando termina en `./quota_watch.sh` sin `&& ./v5run.sh`; sin crontab ni launchd. Confirma R4: lanzamiento manual.

## B. Afirmaciones del código verificadas (para el roadmap)

- `database.py:50` SCHEMA_VERSION=3; `:641-642` y `:667` "SCHEMA_VERSION stays 2" (R6 correcto). `config.py:11` referencia `phase1_5/resolution_discovery.py` inexistente (R6 correcto).
- `features.py:74-87` precio por `market_id` y `prices[0]`, sin `token_id` ni `dataset_version`; en cambio `:120-132` sí filtra outcomes por `dataset_version` (R17 correcto). `strategy_a.py:203-217` guard; `:251,265` `edge_net=None` (R11/R20 correctos).
- `validate_2d.py:286` `validated = False` (correcto). `probability.py:66-68, 82-85, 89-94` colas 10 % en 1 grado y redondeo implícito [t−0.5,t+0.5] (R12 correcto).
- `discovery.py:129-131,158` endDate solo en `source_timestamps` (R8 correcto).
- `.gitignore:51-52` `results/` y `*.duckdb` (correcto). No existe `docs/` en el repo.

## C. Omisiones del roadmap (subsistemas o hechos no cubiertos por ningún lector)

1. **discovery() solo descubre mercados CERRADOS.** OBSERVADO `discovery.py:435`: `base = {"tag_id": tag_id, "closed": "true", ...}`. El modo paper (R23/R24: "catálogo incremental -> precios -> forecasts -> señales") necesita mercados **abiertos** (endDate futuro). No hay ningún ítem que añada descubrimiento de mercados activos ni política de `markets.available_at` para ellos (2B:216-225 lo deja UNKNOWN). Sin esto R24 es imposible. Debe ser un ítem propio (dependencia de R23).
2. **Sonda P1 y la viabilidad del lead son medibles HOY sin red, y el roadmap las aplaza a R8/R9.** OBSERVADO (consulta read-only a `CATALOG_V2.duckdb`): `event_endDate` = 12:00 en **93 221/93 221** mercados (P1 cerrable de inmediato). Y, con `ev.createdAt`: cuantiles de (endDate − createdAt) en horas p01=17.8, p05=34.0, p10=34.9, p25=49.9, **p50=55.9**; **1 782/8 557 eventos (20.8 %) se crearon <48 h antes de endDate**, 155 (<24 h). INFERIDO: a lead 48 h ≈ 21 % del universo no existe en T, y a leads > 56 h más de la mitad. Ningún ítem fija el rango de `lead_hours` factible ni exige cotización + existencia del mercado en T a partir de este dato; R18 debería preregistrar leads con esta tabla y R8 cerrar P1 con estas cifras.
3. **prices.py rechaza puntos en la frontera de ventana.** OBSERVADO `prices.py:83`: `if not (start_ts < ts < end_ts) ... return S_PARSE`; el docstring `:35-37` asume sin evidencia que "the CLOB API excludes request endpoints". Si CLOB devuelve un punto en `t == startTs`, la ventana entera se descarta como PARSE_ERROR y `fetch_history` (`:97-98`) aborta el token. R9 no lo contempla; hay que verificarlo en la muestra real antes del backfill.
4. **Operador de settlement por (fuente, regla), no solo por fuente.** OBSERVADO en CATALOG_V2: `P_NOAA_HourlyData` (1 441) resuelve "off of the Hourly Data" (max de lecturas horarias), distinto de `P_NOAA_TempColumn` (10 087) y de `Y_final = max(cuerpo METAR)` (PREREG_MODELSEL.md:22-24); `P_byForecast` (35 482, 38 % del catálogo) es la plantilla legacy "by the Forecast once finalized" (resolution.py:131-133). El diseño 2E (`:24`) exige predicados de "measurement-rule", pero R12 y R13 solo hablan de WU/NOAA/HKO. Añadir cobertura por `measurement_rule` y por época de plantilla.
5. **Fuente de cuantiles p10..p90.** `weather_forecasts` DDL exige `forecast_p10..p90` y `features.py` los consume, pero R15 ingiere `temperature_2m` determinista y R16 escribe cuantiles "como filas derivadas en weather_forecasts con record_version nuevo". OBSERVADO: la PK de `weather_forecasts` es `(station, model, issue_time, target_date, dataset_version, record_version)` sin campo que distinga "fila cruda" de "fila derivada"; `latest_asof` particiona por `(station, model, target_date)` y tomaría la mayor `record_version`. INFERIDO: hace falta decidir (R16) cómo se marca la derivación (`source`/`model` distinto o `derived_from_dataset_version`) para no confundir crudo y derivado; hoy no está en el criterio.
6. **Sin tabla `labels` ni `station_registry` en el esquema; `paper_trades` no enlaza con `signals`.** OBSERVADO DDL: `paper_trades` tiene `backtest_id/market_id/token_id` pero ningún `signal_id`/`prediction` lineage; `backtest_results` no tiene columna para el hash del preregistro (R19 lo pide "en cada fila": debe ir en `parameters` JSON o requiere migración v4, que 2D §V prohíbe tocar sin reporte). R7/R14/R19 implican migración v4 y ningún ítem la nombra explícitamente.
7. **Coste de ingestión Open-Meteo no cuantificado.** `STATIONS_TZ.json` solo 28/55 (R7 lo cita) pero además R15 no da presupuesto: 55 estaciones × ~95 días (≥2026-06-03) × runs por modelo. Con cuota diaria ya agotada por ~4 400 peticiones de V5, la "decisión abierta" de plan de API es de hecho el bloqueo crítico de R15/R16/R24 y no tiene ítem con owner y criterio.
8. **Fuera de git sigue todo lo de investigación.** OBSERVADO: `~/pmw-e2` sin `.git` (159 ficheros, incl. `REFUTATION_D1_01.md`, `COORDINATE_SENSOR_VERIFICATION.md` nuevos a las 21:36-21:41); constructores del catálogo en `~/.claude/jobs/324ffe40/tmp` (128 entradas, muchas descargas FAA/HOMR del mismo día). R0 lo cubre, pero el criterio de hecho no menciona los ficheros nuevos ni `WF_*_results.json`.
9. **Artefacto de Actions con crash es la única "validación real" y no está en el repo.** OBSERVADO `~/Downloads/phase-2b-validation-report/PHASE_2B_VALIDATION_REPORT.md:3-14`: commit 15a3147, error "Binder Error: dataset_version not found"; DBs con markets=7. R10 lo cubre; conviene que su criterio exija adjuntar este informe como histórico.

## D. Ítems que asumen algo no verificado

- **R9/R22**: rate limits de CLOB y `CLOB_WS_URL` (config.py:46 = None) sin verificar — correctamente marcados como bloqueo.
- **R8**: cita `PHASE_2E_LEAD_HOURS_ANCHOR.md` E1-E7 (backtest.py, validation_oos.md del "proyecto original") que no existen en ningún árbol; la ratificación se apoya en evidencia externa no reproducible. INFERIDO: el helper `prediction_time_for` debe re-derivarse del catálogo (ahora posible, ver C.2), no de E1-E7.
- **R11 (Claude)**: "documentar con fuentes públicas de Polymarket la fórmula de fee" — no hay evidencia en el repo de que esa documentación exista; si no se encuentra, R11 debe tener rama alternativa (fee_status=ESTIMATED con intervalo) o el veredicto de R21 nunca podrá ser neto.
- **R13**: asume que IEM cubre los station-days del catálogo (≥50 estaciones); OBSERVADO `MTARCH_COVERAGE.json` 25/52 y `MODELSEL_GEOVAL_V3_RAW` con y NULL en ZSJN (192 filas). La cobertura IEM por estación no está medida para las 55; criterio "≥50 estaciones" es una suposición.
- **R24**: "N>=14 días" INFERIDO; requiere host persistente (decisión Hetzner/Mac/Actions sin owner).

## E. ¿Es correcta la definición de COMPLETO para un agente en modo paper?

Es razonable pero incompleta en cuatro puntos:

1. **No exige descubrimiento de mercados abiertos ni ejecución en tiempo real del as-of** (cadena diaria). Un backtest sobre catálogo cerrado + un "paper run" que reutilice datos históricos no prueba operación paper. Añadir: "el pipeline paper consume mercados con endDate futuro descubiertos en el día y forecasts obtenidos ANTES de T (captura prospectiva, alternativa C de REVISION_IMPACT_AUDIT.md:83-85)".
2. **No fija criterios de parada/kill-switch ni tolerancia de fallos** (429 sostenidos, días perdidos, tasa de exclusión fail-closed). Para paper, "N días sin intervención" debe definir qué cuenta como día válido (R24 lo insinúa; la definición no).
3. **Exige "fees confirmadas"** (punto 8) — si la semántica no es confirmable públicamente, COMPLETO queda bloqueado por una fuente externa; mejor exigir "modelo de coste preregistrado con sensibilidad" y reportar PnL neto bajo hipótesis declaradas.
4. **Es correcto** excluir dinero real, wallet y Strategy B, y exigir preregistro + informes hasheados versionados; falta indicar que la evidencia debe vivir en git (docs/validation/) — está en las decisiones abiertas, no en la definición.

## F. Lista consolidada de correcciones

| # | Ítem | Corrección |
|---|---|---|
| 1 | estado_actual | Rama `codex/prices-ingestion-wip@6ed85ad` pusheada; 131 passed/2 skipped; Codex sin cuota hasta 2026-10-05 (D3/D5/D6); snapshot v1.1 (54/1); V5.3 emitida (`e8302dc1…`). |
| 2 | R0 | Reducir a: traer `PHASE_2E_LEAD_HOURS_ANCHOR.md`, abrir PR de la rama codex, versionar `~/pmw-e2` (incluidos ficheros de 21:2x-21:4x) y `enum_v2.py/desc.py`. |
| 3 | R1 | Reescribir: Codex inoperativo; registrar reparto bajo D5 con refutador adversarial; renombrar colisión D1 sigue vigente. |
| 4 | R2 | Marcar hecho salvo `V5_STATUS.md` y smoke de 2-3 pares en `v5run.sh`. |
| 5 | R3 | Añadir reconciliación `ebe21014…` (v1.0 desaparecido) vs `071b142c…` (v1.1). |
| 6 | R7 | 54 CANONICA + 1 EXCEPCION (OPKC); registrar alcance de verificación (11/55). |
| 7 | R8 | Cerrar P1 ya (100 % 12:00Z) y añadir tabla de existencia del mercado en T por lead (20.8 % <48 h). |
| 8 | R9 | Quitar "un-skip test_market_asof"; añadir verificación de puntos en frontera (`prices.py:83`). |
| 9 | R12/R13 | Operadores por (source, measurement_rule): WU-byForecast, WU-DailyObs, NOAA-TempColumn, NOAA-HourlyData, HKO. |
| 10 | R16 | Definir cómo se distinguen filas derivadas en `weather_forecasts` (PK sin discriminador). |
| 11 | Nuevo | Descubrimiento de mercados abiertos + `markets.available_at` para paper (dep. de R23). |
| 12 | Nuevo | Migración v4 explícita (station_registry, labels, lineage paper_trades↔signals, hash prereg). |
| 13 | Nuevo | Plan de cuota Open-Meteo con owner y criterio; sin él R15/R16/R24 no tienen ruta. |
| 14 | Definición COMPLETO | Añadir descubrimiento abierto, captura prospectiva, criterios de parada, y fees como modelo preregistrado (no "confirmadas"). |

Rutas clave: `/Users/mariaaleu/workspace/Polymarket_Weather_Agent/src/weather_agent/polymarket/{discovery.py:435,prices.py:83}`, `.../database.py:50,641`, `/Users/mariaaleu/pmw-e2/{DECISIONS.md:49-98,PREREG_MODELSEL_V5.md:218,375,417-423,v5uni.py:6-7,43-45,STATION_COORDS_SNAPSHOT_v1.json,V5_STATUS.md}`, `/Users/mariaaleu/pmw-catalog-v2/CATALOG_V2.duckdb` (tablas ev, v3, dsc), `/private/tmp/pmw-publish/PHASE_2E_LEAD_HOURS_ANCHOR.md`, `~/Downloads/phase-2b-validation-report/PHASE_2B_VALIDATION_REPORT.md`.